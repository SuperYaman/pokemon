{% extends 'base.html.twig.' %}
{% block title %}

{% endblock %}

{% block body %}
    {{ form_start(typeForm) }}
    {{ form_row(typeForm.type) }}
    <button class="btn btn-success" type="submit">Créer</button>
    {{ form_end(typeForm) }}
{% endblock %}



